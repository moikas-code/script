fn process_i32(x: i32) -> i32 {
    return x * 2;
}

fn test_complex_scenarios() {
    // Array of function results
    let results = [
        process_i32(1),
        process_i32(2),
        process_i32(3)
    ];  // Type: [i32]
}